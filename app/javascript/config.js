// app/javascript/config.js
export const config = {
  // World configuration
  worldWidth: 2000,
  worldHeight: 1500,
  
  // Player configuration
  playerSize: 32,
  playerSpeed: 4,
  movementThrottle: 16
};